# benchmark/results

This directory will hold per-release benchmark JSON files written by
`.github/workflows/eval.yml` once the eval suite goes blocking
(Phase 1, PR-B). Until then it is intentionally empty.

Format documented at `docs/benchmarks/index.md`.

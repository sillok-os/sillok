# sillok.jikji — Typed Pack Registry

Pydantic-validated pack metadata + composition rules + retrieval_plan declarations. Implements Top 10 Feature #3 (registry side).

Schema at `sillok/schemas/pack.py` (after F0.5).

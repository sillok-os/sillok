# sillok.naru — Two-Stage Routing

Tier-1 keyword/regex match + Tier-2 LLM intent classification. Implements Top 10 Feature #2.

Real implementation lands during F0.3 cherry-pick from upstream `aipm/scripts/prompt-router-2tier.py`. Current contents are stubs only.
